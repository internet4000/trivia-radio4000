
{{#if model.players}}
{{else}}
  <p>Add the first player:</p>
{{/if}}

{{add-player onSubmit=(route-action 'addPlayer')}}

{{#if model.players}}
  <ol>
    {{#each model.players as |player index|}}
      <li>
        {{input value=player.name}}
      </li>
    {{/each}}
  </ol>

  <button onclick={{route-action "continue"}}>
    Continue
  </button>
{{/if}}

{{outlet}}